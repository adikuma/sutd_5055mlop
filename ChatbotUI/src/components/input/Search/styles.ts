export const search = (fullWidth?: boolean) => ({
  display: 'flex',
  alignItems: 'center',
  width: fullWidth ? '100%' : 'auto'
})

export const search_field = {
  flexGrow: 1
}

export const search_icon_button = {
  mt: '-1rem'
}
