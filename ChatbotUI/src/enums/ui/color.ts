export enum Default {
  DEFAULT = 'default',
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  ERROR = 'error',
  WARNING = 'warning',
  INFO = 'info',
  SUCCESS = 'success'
}

export enum DefaultInherit {
  DEFAULT = 'default',
  INHERIT = 'inherit',
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  ERROR = 'error',
  WARNING = 'warning',
  INFO = 'info',
  SUCCESS = 'success'
}

export enum Inherit {
  INHERIT = 'inherit',
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  ERROR = 'error',
  WARNING = 'warning',
  INFO = 'info',
  SUCCESS = 'success'
}

export enum Link {
  DEFAULT = 'default',
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  INHERIT = 'inherit',
  TEXT_PRIMARY = 'textPrimary',
  TEXT_SECONDARY = 'textSecondary',
  ERROR = 'error'
}

export enum Pagination {
  STANDARD = 'standard',
  PRIMARY = 'primary',
  SECONDARY = 'secondary'
}

export enum Text {
  DEFAULT = 'default',
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  TEXT_PRIMARY = 'textPrimary',
  TEXT_SECONDARY = 'textSecondary',
  DANGER = 'danger',
  ERROR = 'error',
  WARNING = 'warning',
  INFO = 'info',
  SUCCESS = 'success'
}
